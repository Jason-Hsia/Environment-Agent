# Security Policy

## 报告安全问题

请优先使用 GitHub 仓库的 **Security** 页面私下报告漏洞。如果该入口不可用，可以创建不包含利用细节、密钥、个人信息或生产数据的 Issue，说明需要私下联系。

请勿在 Issue、讨论、截图或日志中提交 API Key、访问令牌、Cookie、用户标识和真实问答记录。

## 密钥处理

仓库只提供空值示例配置。开发密钥应写入被 Git 忽略的 `.dev.vars` 或 `.env` 文件；部署密钥应保存为托管平台的 Secret。怀疑密钥泄露时，请立即在对应服务商后台撤销并重新生成。

## 支持范围

安全报告应针对当前默认分支。第三方模型服务、官方标准来源网站和托管平台自身的问题，应同时按照相应服务商的安全渠道报告。
